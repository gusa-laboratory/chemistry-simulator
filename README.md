# 化学シミュレーター｜Interactive Lab

高校／IB化学の授業投影用インタラクティブ教材。スライダーやドラッグで動かすと、グラフ・数値・粒子モデル・3D構造が同時に変化する。各シミュレーターは**単一HTML・依存ライブラリの外部読み込みなし**で、ブラウザで開くだけ・オフラインでも動く。PC／タブレット／スマホのブラウザに対応（レスポンシブ）。

## 収録

| ファイル | 内容 |
|---|---|
| [`index.html`](index.html) | ハブ（入口）。10のシミュレーターへのリンク。**化学基礎 / 化学 / IB** の3区分に分けて並べている |
| [`atomic-ionic-radii.html`](atomic-ionic-radii.html) | **原子半径・イオン半径**: 周期2・周期3の原子、等電子イオン、原子とイオンを電子配置・核電荷・引力矢印とともに比較。自由選択、表示順変更、半径値・電子・核電荷の表示切替に対応 |
| [`molecular-shapes.html`](molecular-shapes.html) | **分子の形**（12分子＋2比較）: 構造式／電子式と3D VSEPRモデルを対応表示。電子対・反発矢印・結合角の表示切替、回転・拡大、分子間比較に対応 |
| [`crystals.html`](crystals.html) | **結晶格子**（6タブ）: 体心立方(BCC)／面心立方(FCC)／六方最密(HCP)／ダイヤモンド構造／NaCl 型／CsCl 型。単位格子で「切り取った」本物の3D断面（頂点＝1/8球・面＝半球）、充填率、ダイヤの4分割による1辺の導出、イオン結晶の限界半径比まで |
| [`stoichiometry.html`](stoichiometry.html) | **化学反応の量的関係**（4タブ）: mol という数え方／分子の中の原子／係数と粒子の個数／過不足がある反応。粒子モデルをボタン操作で1組ずつ反応させ、mol と係数の意味を「数える体験」として理解する |
| [`gas-laws.html`](gas-laws.html) | **気体の法則**（8タブ）: ボイル／シャルル／ゲイリュサック／理想気体／ボイル＋蒸気圧／シャルル＋蒸気圧／圧縮係数 Z–P（van der Waals）／ゲイリュサック＋蒸気圧 |
| [`solutions.html`](solutions.html) | **希薄溶液・コロイド**（7タブ）: 蒸気圧降下・沸点上昇／凝固点降下／浸透圧／疎水コロイド（DLVO・凝析）／電気泳動／親水コロイド（塩析）／保護コロイド |
| [`reaction-rate.html`](reaction-rate.html) | **反応速度と化学平衡**（11タブ）: 衝突しても反応しない／活性化エネルギー／温度とエネルギー分布／濃度と反応の速さ／触媒／速さがつり合うと平衡／平衡定数 K／濃度による移動／圧力・体積による移動／温度による移動／ハーバー・ボッシュ法。教科書の「反応速度と化学平衡」の章にあたる範囲を1ページに収めてある |
| [`coordination-compounds-3d.html`](coordination-compounds-3d.html) | **配位化合物の立体構造**: ジアンミン銀(I)、Cu 錯体（高校表示／配位水表示）、Zn・Al の正四面体、ヘキサシアニド鉄(III)、cis–trans 異性を2D構造式と回転可能な3Dモデルで対応表示 |
| [`organic-compound-stereochemistry.html`](organic-compound-stereochemistry.html) | **有機化合物の立体構造**（9タブ）: アルカン・アルケン・アルキンの結合形、シス–トランス異性、光学異性、エナンチオマー／ジアステレオマー、メソ体、シクロヘキサン配座を2D構造式と回転可能な3Dモデルで比較 |
| [`analytical-spectroscopy.html`](analytical-spectroscopy.html) | **機器分析（IR・MS・NMR）**（5タブ）: IRスペクトルと結合振動／質量スペクトルとフラグメント／¹H NMR／¹³C NMR／未知試料の構造決定。エタノール・プロパノン・エタン酸・トルエン・酢酸エチルなどのスペクトルを読み解き、構造決定まで練習できる（※模式データ） |

表の並びはハブ `index.html` のカード順に合わせている。ハブでは 化学基礎（原子半径〜量的関係）／化学（気体の法則〜有機化合物の立体構造）／IB（機器分析）の3区分に分けている。

旧 `equilibrium.html`（化学平衡・7タブ・旧スタイル）は `reaction-rate.html` に統合して削除した。内容は git 履歴に残っている。

## 作りかけ（ハブ非掲載）

| ファイル | 内容 |
|---|---|
| [`acid-base.html`](acid-base.html) | **酸・塩基と中和滴定**（4タブ）: 中和とは／強酸を滴定する／弱酸を滴定する／指示薬の選び方。①は OH⁻ を1個ずつ加えて H⁺ が水に変わる粒子モデル（pH のグラフはボタンで出し入れ）。②③は1滴ずつ落として滴定曲線が1点ずつ描かれ、グラフの右下にビーカーの中身を併記する。弱酸では電離していない分子が残る様子が見え、半当量点で CH₃COOH : CH₃COO⁻ = 6 : 6、pH = pKa になる。④は変色域を曲線に帯で重ね、跳びの範囲に入るかで使える／使えないを判断させる。**`template.html` を土台にした最初の教材** |

**`index.html` からリンクしていないので、URL を直接開いたときだけ到達できる。** 授業で使って問題がなければ、`index.html` の「化学基礎」の末尾にカードを追加して公開する。

物理の要点・設計判断は Notion「化学シミュレーター教材（希薄溶液・気体の法則）開発ログ」を正本とする。

## ローカルで動かす

各HTMLはブラウザで直接開いても動くが、相対リンク（ハブ↔各ページ）を含むためローカルHTTPサーバー経由が安全。

```bash
python -m http.server 8137
# → http://localhost:8137/
```

（このリポジトリを含む作業環境では `.claude/launch.json` の `chemistry-simulator` を preview_start で起動できる。）

## GitHub Pages で公開

1. このフォルダを独立リポジトリとして push。
2. GitHub の Settings → Pages で `main` ブランチ／ルートを公開元に設定。
3. 公開URL（`https://<user>.github.io/chemistry-simulator/`）を開く。
   - 公開中: <https://gusa-laboratory.github.io/chemistry-simulator/>

`.nojekyll` を置いているので Jekyll 処理はスキップされる。ファイル追加・変更は push すればそのまま反映される（キャッシュ版管理は不要）。

## 新しい単元を作るとき

`template.html` をコピーして中身を書き換える。共通の見た目と定型スクリプトは
以下のファイルが持っているので、教材固有の部分だけ書けばよい。

| ファイル | 役割 |
|---|---|
| `tokens.css` | 色・影・書体の一次情報源（`--t-*`）。**色を直接書かずここを参照する** |
| `ui.css` | 全ページ共通のUI（ヘッダー・タブ・カード・スライダー・全画面表示） |
| `ui.js` | 定型スクリプト（`ChemUI.tabs` / `.fullscreen` / `.bindRange` / `.setRange`） |
| `print.css` | 授業プリントとして印刷するための指定（`media="print"`。画面表示には影響しない） |
| `site-shell.css` / `site-shell.js` | 最後に効かせる調整とタブのキーボード操作 |

読み込み順は `tokens.css` → `ui.css` → ページ固有の `<style>` → `site-shell.css`。
**この順を変えないこと。**

注意点が2つある。

- **折り返し（`@media(max-width:1000px)`）はページ側に書く。** `ui.css` はページの
  `<style>` より先に読まれるため、ページが自分で持つ `.layout` の指定に同じ詳細度で
  負けて効かない。共通ファイルに上げてよいのは、ページが上書きしないものだけ。
- **スライダーの値をコードから変えるときは `ChemUI.setRange(id, 値)` を使う。**
  `input.value = 300` では `input` イベントが飛ばず、数値表示が古いまま残る。

`tokens.css` を参照していない色がページの `:root` に残っている場合、`統一候補:` という
コメントが付いている。これはページ間で値がズレているもの（背景 `#eef3f9` と `#f3f6fb`、
補足文字色が4系統、目盛線 `#e8eef6` と `#e8eef7` など）で、見た目を変えないため
移行時は据え置いた。統一するかは別途判断する。

## 技術メモ

- **共通アーキテクチャ（2Dグラフ系）**: SVG を `innerHTML` にテンプレートリテラルで文字列生成→`input` イベントで全再描画。決定的疑似乱数（LCG・seed固定）で粒子配置が再描画で飛ばない。共通部品 `frame()` / `polyline` / `attachHover()` / `setReadout()`。
- **設計方針（`stoichiometry.html` 以降）**: 説明文は極力置かず「図と数字だけ」にする。グラフより粒子モデルを主役に。スライダーよりクリックで段階を進める。式の真下に分子を置く。初期値は必要最低限。絵と数値を必ず一致させる。
- **アニメーションは時刻の関数にする**: `reaction-rate.html` ⑥では全コマを先に作り置きし、状態を時刻 t から引く。再描画で絵が変わらず、`requestAnimationFrame` も不要（ブラウザプレビューは `document.hidden` 扱いで rAF が発火しないため、rAF に頼ると動作確認ができない）。再生はコマ間隔のタイマーで駆動する。
- **`<input type=range>` の落とし穴**: `value` は `step` に丸められるので、1フレーム分の細かい増分を加算する用途には使えない。時刻は JS 変数で持ち、スライダーは表示専用にする。
- **結晶格子（3D）**: three.js（r128）＋ OrbitControls を**HTMLにインライン埋め込み**（CDN不要・オフライン可）。原子球を単位格子の面でクリッピング（頂点1/8球・面半球）し、各面にキャップ円盤を貼って平らな断面を作る。
- **全画面授業モード**（`requestFullscreen` → `body.is-fullscreen` でレイアウト組み替え）、2Dグラフ⇄粒子モデルのスワップを装備。
- **レスポンシブ**: 各ページ `@media` でタブレット／スマホに対応（ヘッダー圧縮、全画面ボタンのアイコン化、タブ・操作ボタンの小型化、模式図の高さ制限、3D はタッチ回転対応）。
- **配布物**: 各シミュレーターHTML＋ハブ `index.html`＋`icons/icon-192.png`（favicon）。※PWA（Service Worker・manifest・インストール導線）は方針変更により撤去済み。
- **`sw.js` は撤去の残骸ではない**: 旧PWA時代に Service Worker を登録した端末が今も残っているため、それを解除し古いキャッシュを消す**キルスイッチ**として現役で置いてある（新規訪問者はどのページからも SW を登録しないので無害）。掃除が行き渡ったと判断できたら削除してよい。
